<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp" >

    <CheckBox
        android:id="@+id/servicetypecb"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:focusable="false"
        android:focusableInTouchMode="false"
         />

    <TextView
        android:id="@+id/code"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/servicetypecb"
        android:layout_alignBottom="@+id/servicetypecb"
        android:layout_toRightOf="@+id/servicetypecb"
        android:text="TextView" />

   <LinearLayout
       android:layout_below="@id/code"
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:orientation="vertical">

       <EditText
           android:id="@+id/edit_partname"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:layout_margin="5dp"
           android:layout_weight="1"
           android:background="@drawable/roundbutton"
           android:hint="@string/partname"
           android:inputType="text"
           android:padding="16dp"
           android:visibility="gone" />
       <TextView
           android:id="@+id/tilTest"
           android:layout_marginStart="20dp"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:text="Error"
           android:textColor="@color/colorRed"
           android:textSize="12dp"
           android:visibility="gone"/>

   </LinearLayout>

</RelativeLayout>